//while loop
console.log("While Loop");
var repo=[1,2,3,4,5,6,7,8,9]
while (repo.length<=repo.length) {
    console.log("Push the Data to Repository");
    if (repo.length>repo.length) {
        console.log("Repo is Full");
        break;
    }
    break;
}